#!/usr/bin/env python3
"""concat two strings
The type of both strings should be str."""


def concat(str1: str, str2: str) -> str:
    """concat two strings
    The type of both strings should be str."""
    return str1 + str2
